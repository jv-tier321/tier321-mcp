{
  "name": "@tier321/mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env \"\" --strict",
    "deploy:staging": "wrangler deploy --env staging --strict",
    "build:staging": "wrangler deploy --env staging --dry-run --outdir=dist-staging",
    "smoke:staging": "node scripts/smoke-staging.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "verify:parity": "tsx scripts/verify-parity.ts",
    "build": "wrangler deploy --env \"\" --dry-run --outdir=dist",
    "types": "wrangler types src/worker-configuration.d.ts --include-runtime=false --strict-vars=false",
    "check": "npm run typecheck && npm test && npm run verify:parity && npm run build",
    "audit": "npm audit --audit-level=low",
    "verify:release": "node scripts/verify-release.mjs",
    "package:release": "node scripts/package-release.mjs"
  },
  "dependencies": {
    "@cfworker/json-schema": "4.1.1",
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@cloudflare/workers-types": "5.20260917.1",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.134.0"
  },
  "engines": {
    "node": "^22.0.0 || ^24.0.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
